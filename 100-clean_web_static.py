import os

def do_clean(number=0):
    if (number < 2):
        arch_files = [file for file in os.listdir('versions/') if file[-.endswith('.tgz')]
        filter_archives = arch_files.split("_")


        
           
    else:


